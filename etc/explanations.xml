<?xml version="1.0"?>
<!--
/**
 * Copyright © Magento, Inc. All rights reserved.
 * See COPYING.txt for license details.
 */
-->
<config xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:noNamespaceSchemaLocation="urn:magento:module:Magento_Store:etc/config.xsd">
    <default>
        <crealoz_audit>
            <translations>
                <hardWrittenSQLSelect>
                    <title translate="true">Hard Written SQL SELECT</title>
                    <explanation translate="true">SELECT queries must be avoided. Use the Magento Framework methods instead or a custom repository with a getList and/or getById methods.</explanation>
                </hardWrittenSQLSelect>
                <hardWrittenSQLDelete>
                    <title translate="true">Hard Written SQL DELETE</title>
                    <explanation translate="true">DELETE queries must be avoided. Use the Magento Framework methods instead or a custom repository with a delete and/or deleteById methods.</explanation>
                </hardWrittenSQLDelete>
                <hardWrittenSQLUpdate>
                    <title translate="true">Hard Written SQL UPDATE</title>
                    <explanation translate="true">UPDATE queries should be avoided. Use the Magento Framework methods instead or a custom repository with a save method. It can be faster for large amounts of data but it can lead to data loss in case of an update.</explanation>
                </hardWrittenSQLUpdate>
                <hardWrittenSQLInsert>
                    <title translate="true">Hard Written SQL INSERT</title>
                    <explanation translate="true">INSERT queries should be avoided. Use the Magento Framework methods instead or a custom repository with a save method. It can be faster for large amounts of data but it can lead to data loss in case of an update.</explanation>
                </hardWrittenSQLInsert>
                <hardWrittenSQLJoin>
                    <title translate="true">Hard Written SQL JOIN</title>
                    <explanation translate="true">JOIN queries should be avoided. Use the Magento Framework methods instead.</explanation>
                </hardWrittenSQLJoin>
            </translations>
        </crealoz_audit>
    </default>
</config>